{
  "name": "r2-registry",
  "workers_dev": true,
  "main": "../index.ts",
  "compatibility_date": "2024-09-09",
  "compatibility_flags": ["nodejs_compat"],
  "env": {
    // Production
    "production": {
      "r2_buckets": [{ "binding": "REGISTRY", "bucket_name": "" }],
      "vars": {
        "JWT_REGISTRY_TOKENS_PUBLIC_KEY": "",
      },
    },
    "dev": {
      "r2_buckets": [{ "binding": "REGISTRY", "bucket_name": "r2-image-registry-dev" }],
      "vars": {
        "USERNAME": "hello",
        "PASSWORD": "world",
        // Tests opt into edge caching explicitly by overriding this per-request,
        // so that cached reads can't mask R2 state changes in unrelated tests.
        "EDGE_CACHE": "off",
      },
    },
  },
}
